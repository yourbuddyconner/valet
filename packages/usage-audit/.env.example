# Copy to .env.local; Bun auto-loads it from CWD when you run `pnpm audit`.

CF_API_TOKEN=
CF_ACCOUNT_ID=
ANTHROPIC_API_KEY=

# D1 database IDs per env. --env dev|prod selects which one is used.
D1_DATABASE_ID_DEV=
D1_DATABASE_ID_PROD=
