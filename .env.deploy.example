# Deployment Configuration
# Copy to .env.deploy and fill in your values.
# This file holds deployment-specific config (not secrets).
# Secrets should be set via `wrangler secret put` or Modal environment variables.

# Cloudflare Worker production URL
WORKER_PROD_URL=https://your-worker.your-subdomain.workers.dev

# Cloudflare Pages project name (must be globally unique across Cloudflare Pages)
PAGES_PROJECT_NAME=my-valet

# Modal backend URL template ({label} is replaced per-endpoint)
MODAL_BACKEND_URL=https://your-modal-workspace--{label}.modal.run

# D1 database ID (from `wrangler d1 create <name>`)
D1_DATABASE_ID=your-d1-database-id

# R2 bucket name
R2_BUCKET_NAME=valet-storage

# Comma-separated email allowlist for the worker
ALLOWED_EMAILS=you@example.com

# Modal deploy command (uses uv to run modal from the backend project)
MODAL_DEPLOY_CMD=uv run --project backend modal deploy
